name: botwa
type: web
regions:
  - frankfurt
build:
  install:
    command: npm install
  build:
    command: echo 'no build needed'
  run:
    command: node index.js
ports:
  - internal: 3000
    external: 80
    protocol: http
